import { Factory } from './factory';

// For now we will use the Factory type as a placeholder for the Report type
// In the future, we might want to create a more specific Report type that extends Factory
// or includes additional fields specific to reports.

export type Report = Factory;
